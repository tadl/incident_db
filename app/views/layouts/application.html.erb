<!DOCTYPE html>
<html>
  <head>
    <title>TADL Incident DB <% if @page_title %> - <%= @page_title %> <% end %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.2/dist/umd/popper.min.js" crossorigin="anonymous"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/moment.js/2.29.4/moment.min.js"></script>
    <script src="https://cdn.jsdelivr.net/gh/Eonasdan/tempus-dominus@master/dist/js/tempus-dominus.js" crossorigin="anonymous"></script>
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <div class="container">
        <a class="navbar-brand" href="/">
          <%= icon('fa-solid', 'skull-crossbones') %>
          TADL IncidentDB
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <li class="nav-item">
            <a class="nav-link white-link" href="/incidents/new">New Incident</a>
          </li>
          <li class="nav-item">
            <a class="nav-link white-link" href="/incidents">Incidents</a>
          </li>
          <li class="nav-item">
            <a class="nav-link white-link" href="#">Patrons</a>
          </li>
          <li class="nav-item">
            <a class="nav-link white-link" href="/admin">Admin Pannel</a>
          </li>
        </div>
        <% if current_user %>
            <form class="d-flex  ms-auto" role="search">
              <span style="margin-right: 10px; color: white"><%=current_user.name %></span>
              <a class="btn btn-danger btn-sm nav-item" href="/signout">Logout</a>
            </form>
          <% else %>
            <%= button_to "Staff Login", "/auth/google_oauth2", method: :post, class: "btn btn-primary" %>
        <% end %>
      </div>    
    </nav>
    <div class="container mt-2">
      <%= yield %>
    </div>
  </body>
</html>
